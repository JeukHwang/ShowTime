// import { viewRecorder } from "./functions/recorder";
// import { log } from "./functions/util";

chrome.runtime.onInstalled.addListener(() => {
    console.log('ViewTime is installed!');
    // log("log", viewRecorder.load());
});